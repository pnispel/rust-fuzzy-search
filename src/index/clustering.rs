use util::ld;
use std::collections::hash_map::{HashMap, Entry};
use std::ascii::AsciiExt;
use std::cmp::Ordering;

pub fn compute_cluster(test: &mut &str) {
  // ClusterLink::remove_all();
  // Cluster::remove_all();
}
